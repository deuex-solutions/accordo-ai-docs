# Domain Docs

This project uses a **multi-context** layout.

- Root level documents describe global architecture.
- Per-service directories (e.g., `Accordo-ai-backend`, `Accordo-ai-frontend`) contain their own `CONTEXT.md` and `docs/adr/`.
